import os, inspect, os.path as path, futils
Import('build')

rtdir = build.runtime_build_dir
futils.CopyTree(path.join('poco', 'bin'), rtdir)
futils.CopyTree(path.join('webkit', 'bin'), rtdir)
futils.CopyToDir(path.join('microsoft', 'Microsoft.VC80.CRT'), rtdir)

