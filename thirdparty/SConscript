#!/usr/bin/env python
import os, inspect, os.path as path
import distutils.dir_util as dirutil

Import('build')
print "Copying thirdparty resources...",
SConscript('osx/Sconscript')
#kif build.is_linux() and build.arch == '64':
#k	SConscript('linux64/Sconscript')
#k
#kelse:
#k	SConscript(path.join(build.os, 'Sconscript'))
print "done"
