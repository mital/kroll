import os, inspect, os.path as path, utils
Import('build')

rtdir = build.runtime_build_dir
for framework in Glob('*/*.framework'):
	build.utils.CopyToDir(framework, rtdir)

for libdir in Glob('*/lib'):
	build.utils.CopyTree(libdir, rtdir)


